package ADT;

/**
 *
 * @author user
 */
public interface NodeReference {
    // A hollow interface that does nothing but act as a "Label", IMPORTANT
    // to avoid using Object and using extra generic type in parameter in listinterface doubly listed list or else
}
